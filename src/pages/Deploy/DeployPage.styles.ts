import styled from "styled-components";

export const StyledWrapper = styled.div`
    display: flex;
    flex-direction: column;
`

export const StyledDeployWrapper = styled.div`
    display: flex;
    flex-direction: row;

    padding-top: 1rem;
`